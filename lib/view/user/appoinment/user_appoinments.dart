import 'package:flutter/material.dart';

class UserAppoinments extends StatelessWidget {
  const UserAppoinments({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('tyft')));
  }
}
