import 'package:flutter/material.dart';

class AppColors {
  // Base Surfaces
  static const Color surface = Color(0xFFF8F9FA);
  static const Color surfaceLow = Color(0xFFF3F4F5);
  static const Color surfaceLowest = Color(0xFFFFFFFF);
  static const Color surfaceHigh = Color(0xFFE7E8E9);

  // Primary (Blue Gradient)
  static const Color primary = Color(0xFF0061A4);
  static const Color primaryLight = Color(0xFF2196F3);

  //  Secondary (Optional Green)
  static const Color secondary = Color(0xFF4CAF50);
  static const Color secondaryLight = Color(0xFFA5D6A7);

  //Text Colors
  static const Color textPrimary = Color(0xFF191C1D); // on-surface
  static const Color textSecondary = Color(0xFF6B7280);

  // Borders (Ghost Border)
  static const Color borderLight = Color(0x1A000000); // 10% opacity black

  //  Input Background
  static const Color inputFill = Color(0xFFF3F4F5);

  // Disabled
  static const Color disabled = Color(0xFFBDBDBD);
}
